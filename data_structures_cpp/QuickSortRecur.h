#ifndef QUICKSORTRECUR_H
#define QUICKSORTRECUR_H

template <typename T>
void quick_sort(std::vector<T> data){
    std::string s = typeid(T).name();
    std::cout << s << std::endl;
}
#endif //QUICKSORTRECUR_H
